# TRANPASS — Claude 向け引き継ぎ書（設定サポート用）

> このドキュメントは、TRANPASS を導入する方が**設定で困ったときに、その方の Claude に
> 読ませて助けてもらう**ための引き継ぎ書です。Claude はこの内容を前提に、
> **非技術者のユーザーをやさしく日本語で**サポートしてください。

---

## 0. サポートする相手（ユーザー）について
- **非技術者**です。専門用語（デプロイ、スコープ、マニフェスト等）は**かみ砕いて**説明してください。
- **日本語**でやり取りしてください。
- 「TRANPASS.zip」一式と「INSTALL.md（インストールマニュアル）」を手元に持っています。
  まずは **INSTALL.md の手順**に沿って案内するのが基本です。
- エラー時は、**画面に出ている赤い／黒いメッセージの文言をそのまま聞く**のが最短の診断方法です。

## 1. TRANPASS とは（アプリ概要）
- **Google Apps Script (GAS) 製の個人用パスワード管理 Web アプリ。**
- アカウント・パスワード・クレジットカード情報を **AES-256 で暗号化**し、
  ユーザー自身の **Google Drive 上の単一ファイル** `TRANPASS/tranpass.enc` に保存。
- ログインは **0〜9 をシャッフルした数字キーから、設定した4桁 PIN を選ぶ**覗き見対策方式。
- Web アプリは **「自分のみ」** に公開（本人の Google アカウントでのみアクセス可能）。

## 2. ファイル構成（GAS プロジェクト内）
| GAS 上の種類 | 名前 | 役割 |
|---|---|---|
| スクリプト | `Code.gs` | **サーバー側すべて**（認証・データCRUD・AES暗号化・乱数・セルフテスト） |
| HTML | `Index` | 画面の骨組み |
| HTML | `Style` | CSS（デザイン） |
| HTML | `Script` | クライアント JS（画面の動き） |
| マニフェスト | `appsscript.json` | スコープと公開設定（`access: MYSELF`） |

> ⚠️ サーバー側コードは**わざと `Code.gs` 1ファイルに統合**しています。
> 複数ファイルに分けると貼り忘れで `ReferenceError` が起きるためです。提案で分割しないこと。

## 3. 正しいセットアップの要点（ここを外すと失敗する）
1. `Code.gs` は**既存の中身を全消し**してから**全文**を貼る（部分貼りは `ReferenceError` の元）。
2. HTML ファイル名は **拡張子なしで** `Index` / `Style` / `Script`（大文字小文字も一致）。
   - `doGet()` が `createTemplateFromFile('Index')`、`include('Style')`/`include('Script')` で名前参照しているため。
3. `appsscript.json` を表示するには「プロジェクトの設定」で**マニフェスト表示をオン**にしてから貼る。
4. デプロイは **ウェブアプリ / 実行=自分 / アクセス=自分のみ**。
5. 初回は**権限承認**が必要。「確認されていません」画面は **詳細 → 移動 → 許可**（自作アプリなので正常）。
6. **コード修正後は再デプロイ必須**：「デプロイを管理」→ ✏️ → バージョン「新バージョン」→「デプロイ」。URL は不変。

## 4. よくあるトラブルと対処
| 症状 / メッセージ | 原因 | 対処 |
|---|---|---|
| `ReferenceError: ○○ is not defined` | `Code.gs` の貼り付けが不完全 | 中身を全消し → 全文貼り直し → 保存 |
| 画面が真っ白・崩れる / `include` 系エラー | HTML 名の誤り | `Index`/`Style`/`Script`（拡張子なし）に修正 |
| 「承認が必要」が繰り返す / `Authorization is required` | 承認未完了 or マニフェスト未反映 | `appsscript.json` を貼って保存 → 再度承認 |
| Drive 関連の権限エラー | Drive スコープ未承認 | デプロイし直し → 承認をやり直す |
| 直したのに反映されない | 再デプロイしていない | 第3章-6 の再デプロイ手順 |
| `runSelfTest` が `[NG]` | 貼り付け漏れ・改変 | 該当ファイルを貼り直し、再実行 |
| PIN を5回間違えてロック | 連続失敗ロック（5分） | 5分待つ。`LOCK_UNTIL` を消せば即解除も可 |

## 5. しくみ（Claude が原因を推論するための技術メモ）
- 暗号化：**AES-256-CTR（純JS実装）+ HMAC-SHA256（GASネイティブ）= Encrypt-then-MAC**。
  保存形式は base64( `IV(16) || 暗号文 || HMAC(32)` )。
- ハッシュ等は `Utilities`（`computeDigest` / `computeHmacSha256Signature` / `base64*`）を使用。
- **バイト符号変換**に注意：GAS の `Byte[]` は -128..127。内部は 0..255 で扱い、`Utilities` 受け渡し直前に
  signed へ変換（`toUnsigned` / `toSignedBytes`）。ここを壊すと暗号が一致しなくなる。
- **マスターキー**：32バイトを初回自動生成し **Script Properties (`MASTER_KEY`)** に保管。
  本人のプロジェクトに紐づき、アプリ経由でのみ復号。Drive 上は常に暗号文。
- 認証：4桁 PIN を salt + ストレッチ（SHA-256 ×2000）でハッシュ化し `PIN_HASH`/`PIN_SALT` に保存。
  ログイン成功でトークンを UserCache に発行（30分）。

## 6. 設定値の場所（プロジェクトの設定 → スクリプト プロパティ）
| キー | 意味 | 操作 |
|---|---|---|
| `MASTER_KEY` | 暗号化の鍵（32B/base64） | **絶対に削除・変更しない**（＝復号不能） |
| `PIN_HASH` / `PIN_SALT` | PIN のハッシュとソルト | **両方削除すると PIN を再設定**できる（データは保持） |
| `LOCK_UNTIL` / `FAIL_COUNT` | ロック管理 | 削除すればロック即解除 |
| `FOLDER_ID` / `FILE_ID` | Drive の保存先 | 通常は触らない |

## 7. PIN を忘れた / リセットしたいとき（手順をユーザーに案内）
1. GAS エディタ → ⚙️「プロジェクトの設定」→ 下部「スクリプト プロパティ」。
2. `PIN_HASH` と `PIN_SALT` の**2つだけ削除**して保存。
3. アプリの URL を開くと「初回設定」に戻るので、新しい PIN を設定。
   → **`MASTER_KEY` は消さないこと**（消すと保存データが復号できなくなる）。

## 8. 機能の使い方（ユーザーに聞かれたら）
- 追加：右下 **＋** →「通常」または「クレジットカード」。
- サブアカウント：入力画面の「＋ サブアカウントを追加」（例：取引パスワード）。
- 検索：上部ボックス（名称・アカウント・カテゴリー・メモが対象）。
- コピー：各値の **📋**。パスワード/コードは **👁** で一時表示、コピー60秒後に自動クリア。
- 設定 **⚙️**：カテゴリー編集 / PIN 変更 / 暗号化バックアップ。
- iPhone は Safari で開き「ホーム画面に追加」でアプリ風に使える。

## 9. 絶対に守る注意（ユーザーへ強調）
- 🔑 `MASTER_KEY` とプロジェクト本体を**削除しない**（＝全データ復号不能）。
- 🔢 PIN を忘れない（忘れたら第7章でリセット可、ただしデータはマスターキー次第）。
- 💾 ときどき設定画面から**暗号化バックアップ**を取る。

## 10. サポート時のスタンス（Claude へ）
- いきなり高度な改修を勧めず、まず **INSTALL.md の手順のどこで詰まったか**を切り分ける。
- **赤い／黒いエラーメッセージの正確な文言**を聞く。原因の8割はここで判明する。
- 破壊的操作（プロパティ削除・再デプロイ）は、**何が起きるかを一言添えてから**案内する。
- 自信がない・影響が大きい変更は、勝手に進めず**ユーザーに確認**する。
