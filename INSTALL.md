# TRANPASS インストールマニュアル（はじめての方向け）

このガイドは、**Google Apps Script（GAS）を初めて使う方**でも TRANPASS を
自分専用のパスワード管理アプリとして使えるように、画面の流れにそって
ていねいに説明します。むずかしい知識は不要です。順番どおりに進めれば完成します。

所要時間：**約15〜20分**

---

## 0. はじめに

### TRANPASS でできること
- アカウントとパスワードを **暗号化して安全に保存**（自分だけが見られます）
- iPhone / PC / Mac のブラウザから利用可能
- ログインは **毎回ならびが変わる数字キー**から、自分で決めた4桁を選ぶ方式

### 用意するもの
- **Google アカウント**（普段お使いのGmailのアカウントでOK）
- **パソコン**（最初の設定はパソコンが楽です。スマホでも可能ですが手順はPC前提で書いています）
- 同梱の **TRANPASS.zip**（このファイル一式）

### 「GAS」ってなに？
Google が無料で提供している「自分専用の小さなプログラムをGoogle上で動かせる仕組み」です。
サーバーの契約などは不要で、あなたのGoogleアカウントの中で動きます。

---

## 1. ZIPを解凍する

1. お手元の **TRANPASS.zip** をダブルクリックして解凍します。
2. 中に次のファイルが入っています（これらを後でコピー＆ペーストします）。

| ファイル名 | 役割 |
|------------|------|
| `Code.gs` | アプリの本体（プログラム） |
| `Index.html` | 画面 |
| `Style.html` | 見た目（デザイン） |
| `Script.html` | 画面の動き |
| `appsscript.json` | 設定ファイル |

> 💡 ファイルは「メモ帳」や「テキストエディット」で開けます。中身を**全部コピー**して使います。

---

## 2. 新しいプロジェクトを作る

1. ブラウザで **https://script.google.com** を開きます。
2. （初回はGoogleへのログインを求められたらログインします）
3. 左上の **「＋ 新しいプロジェクト」** をクリックします。
4. 画面が開いたら、上部の「無題のプロジェクト」をクリックして、名前を **TRANPASS** に変更します。

これで作業場所ができました。

---

## 3. 本体（Code.gs）を貼り付ける

1. 画面左側に **「コード.gs」**（または `Code.gs`）というファイルが最初からあります。これをクリック。
2. 右側のエディタに最初から書かれている内容（`function myFunction() {}` など）を
   **すべて選択して削除**します（Windows: `Ctrl+A` → `Delete` / Mac: `⌘A` → `Delete`）。
3. 解凍した **`Code.gs` をテキストエディタで開き、中身を全部コピー**します。
4. それを GAS のエディタに **貼り付け**ます。
5. 上部の **💾（保存）** アイコン、または `Ctrl+S` / `⌘S` で保存します。

---

## 4. 画面ファイル（HTML）を3つ追加する

HTMLファイルを3つ作ります。**名前のつけ方が重要**なので、ゆっくり進めましょう。

### Index を作る
1. 左側ファイル一覧の右にある **「＋」** をクリック → **「HTML」** を選びます。
2. ファイル名を聞かれるので **`Index`** と入力します（**拡張子 `.html` は付けません**）。
3. できたファイルの中身を**すべて削除**し、解凍した **`Index.html` の中身を貼り付け**ます。
4. 保存します。

### Style を作る
1. もう一度 **「＋」→「HTML」**。
2. 名前は **`Style`**。
3. 中身を削除して、解凍した **`Style.html` の中身を貼り付け**、保存。

### Script を作る
1. もう一度 **「＋」→「HTML」**。
2. 名前は **`Script`**。
3. 中身を削除して、解凍した **`Script.html` の中身を貼り付け**、保存。

> ✅ ここまでで、左側に `Code.gs` / `Index.html` / `Style.html` / `Script.html` の
> 4つが並んでいればOKです。

---

## 5. 設定ファイル（appsscript.json）を反映する

この設定で「アプリを自分専用にする」「Driveに保存できるようにする」を指定します。

1. 左側の **⚙️「プロジェクトの設定」** をクリック。
2. **「`appsscript.json` マニフェスト ファイルをエディタで表示する」** のチェックを**オン**にします。
3. 左側に **`appsscript.json`** というファイルが現れるのでクリック。
4. 中身を**すべて削除**し、解凍した **`appsscript.json` の中身を貼り付け**ます。
5. 保存します。

---

## 6. （任意・おすすめ）動作テストをする

暗号化が正しく動くか、その場で確認できます。

1. エディタ上部の関数選択メニュー（▼）で **`runSelfTest`** を選びます。
2. **「実行」** をクリック。
3. 初回は「承認が必要」と出ます → 次の **7章** の承認手順を済ませてください。
4. 下部の「実行ログ」に **`[OK]`** がならべば成功です。

> ⚠️ もし `[OK]` が出ずエラーになる場合は、貼り付け漏れがないか確認してください。

---

## 7. アプリとして公開（デプロイ）する

1. 右上の **「デプロイ」** → **「新しいデプロイ」** をクリック。
2. 左上の歯車⚙️（種類の選択）→ **「ウェブアプリ」** を選びます。
3. 設定をこのようにします：
   - **説明**：`TRANPASS`（なんでもOK）
   - **次のユーザーとして実行**：**自分**
   - **アクセスできるユーザー**：**自分のみ** 👈 これが大切（あなた以外は開けません）
4. **「デプロイ」** をクリック。

### 権限の承認（初回だけ）
このとき「承認」を求められます。少しドキッとする画面が出ますが、**あなた自身が作ったプログラム**なので問題ありません。

1. **「アクセスを承認」** をクリック → 自分のGoogleアカウントを選択。
2. **「このアプリは Google で確認されていません」** と出たら、
   左下の **「詳細」** → **「（安全ではないページ）に移動」** をクリック。
   （これは自作アプリで必ず出る案内です）
3. 「TRANPASS が次を実行します…」の確認で **「許可」** をクリック。

### URLを受け取る
承認が終わると **「ウェブアプリ」のURL**（`https://script.google.com/macros/.../exec`）が表示されます。
このURLが **あなた専用のTRANPASS** です。**ブックマーク**しておきましょう。

---

## 8. 初めて使う（PINの設定）

1. 受け取ったURLを開きます。
2. **「初回設定：4桁のPIN（0〜9）を決めてください」** と表示されます。
3. 好きな4桁の数字を、画面の数字キーをタップして入力します。
4. 確認のため**もう一度**同じ4桁を入力します。
5. これで設定完了。次回からはこのPINでログインします。

> 🔐 数字キーは**毎回ならびが変わります**。背後から見られても入力位置から
> PINが推測されにくい仕組みです。

---

## 9. 使い方

| やりたいこと | 操作 |
|--------------|------|
| **追加** | 右下の **＋** → 「通常」か「クレジットカード」を選んで入力 |
| **サブアカウント** | 入力画面の「＋ サブアカウントを追加」（取引パスワード等） |
| **検索** | 上の検索ボックスに名称・アカウントなどを入力 |
| **コピー** | 各項目の **📋** ボタン（パスワードは👁で一時表示、コピー後60秒で自動消去） |
| **編集** | 各カード右下の小さな「✎ 編集」 |
| **カテゴリー変更** | 右上の **⚙️ 設定** → カテゴリーの追加・削除・並べ替え |
| **PIN変更** | 設定画面の「PIN変更」 |
| **バックアップ** | 設定画面の「暗号化バックアップを作成」 |
| **ロック** | 右上の **🔒**（すぐログイン画面に戻る） |

iPhoneでは、Safariで開いて **「ホーム画面に追加」** すると、アプリのように使えて便利です。

---

## 10. あとから修正したいとき（再デプロイ）

プログラムを直したら、**そのままでは反映されません**。次の手順で更新します（URLは変わりません）。

1. ファイルを貼り替えて保存。
2. **「デプロイ」→「デプロイを管理」**。
3. 一覧の右にある **鉛筆 ✏️** をクリック。
4. **「バージョン」を「新バージョン」** に変更。
5. **「デプロイ」** をクリック。
6. いつものURLを開き直すと、更新が反映されています。

---

## 11. 困ったときは（よくあるトラブル）

| 症状 | 原因と対処 |
|------|-----------|
| `ReferenceError: ○○ is not defined` | `Code.gs` の貼り付けが途中。**全部消してから全文を貼り直し** |
| 画面が真っ白／崩れる | HTMLファイルの名前ちがい。`Index` `Style` `Script`（拡張子なし）になっているか確認 |
| 「承認が必要」が何度も出る | 7章の承認をやり直す。`appsscript.json` を貼ったか確認 |
| 直したのに変わらない | **再デプロイ（10章）** をしていない |
| エラーで進めない | 画面に出る**赤いメッセージの文言**を控えておくと原因特定が早いです |

---

## 12. 大切な注意（必ずお読みください）

- 🔑 **マスターキーは消さないでください。** 暗号化の鍵はあなたのプロジェクト内に
  自動生成・保管されます。プロジェクトを削除したり、設定（スクリプトプロパティ）の
  `MASTER_KEY` を消すと、**保存したデータは二度と復号できなくなります。**
- 🔢 **PINを忘れないでください。** 万一忘れた場合は、設定の `PIN_HASH`・`PIN_SALT` を
  消すと再設定できます（データはマスターキーが残っていれば保持されます）。
- 💾 大切な情報なので、ときどき **バックアップ**（設定画面）を取っておくと安心です。

---

これで完了です。おつかれさまでした！🎉
わからない点があれば、画面のメッセージや状況を伝えてもらえれば一緒に解決します。
